# 部署说明

> 最后更新：2026-05-25

---

## 一、仓库全景

本项目（`luoke4.0/`）通过 `gh-pages` 工具构建并推送到以下三个 GitHub 仓库，每个仓库对应一个 GitHub Pages 站点。

| 仓库 | 站点地址 | 用途 | 现状 |
|---|---|---|---|
| [`Luoke-yise-test`](https://github.com/05RubyTang/Luoke-yise-test) | https://05rubytang.github.io/Luoke-yise-test/ | **测试站**（主力开发验证） | ✅ 最新，当前 v4.0 + S2 全功能 |
| [`yise-Luoke-v2.0-ruby-s`](https://github.com/05RubyTang/yise-Luoke-v2.0-ruby-s) | https://05rubytang.github.io/yise-Luoke-v2.0-ruby-s/ | **正式站**（对外分发给用户） | ⚠️ 内容落后，待同步 |
| [`Yise--assistant-s2`](https://github.com/05RubyTang/Yise--assistant-s2) | https://05rubytang.github.io/Yise--assistant-s2/ | S2 开发初期临时测试站 | 🗄️ 基本废弃，0 Stars |

> **注意**：三个仓库均无源码分支，只有 `gh-pages` 分支（存放构建产物）。源码统一在本地 `luoke4.0/` 目录的 `main` 分支维护。

---

## 二、部署流程规范

### 原则：测试站先行，审核后上正式站

```
开发改动
   ↓
npm run deploy:test   →   Luoke-yise-test（测试站）
   ↓
手动打开测试站，验证功能 OK
   ↓
npm run deploy:prod   →   yise-Luoke-v2.0-ruby-s（正式站）
```

**不允许跳过测试站直接部署正式站。**

---

## 三、部署命令

对应 [`package.json`](package.json) 中的 scripts：

```bash
# 部署到测试站（Luoke-yise-test）
npm run deploy:test

# 部署到正式站（yise-Luoke-v2.0-ruby-s）
npm run deploy:prod

# 部署到 S2 早期测试站（基本废弃，一般不用）
npm run deploy:s2
```

各命令的构建配置：

| 命令 | VITE_BASE_PATH | 目标仓库 |
|---|---|---|
| `deploy:test` | `/Luoke-yise-test/` | `Luoke-yise-test.git` |
| `deploy:prod` | `/yise-Luoke-v2.0-ruby-s/` | `yise-Luoke-v2.0-ruby-s.git` |
| `deploy:s2` | `/Yise--assistant-s2/` | `Yise--assistant-s2.git` |

---

## 四、上正式站前的审核清单

在执行 `npm run deploy:prod` 之前，请在测试站（https://05rubytang.github.io/Luoke-yise-test/）逐项验证：

**基础功能**
- [ ] 首页加载正常，无白屏
- [ ] 图片资源正常（精灵图、属系图标、背景图等）
- [ ] Tab 切换（首页 / 方案库 / 我的）正常

**核心刷取流程**
- [ ] 选择方案 → 确认方案 → 进入刷取页，流程畅通
- [ ] 破盾记录：原色 / 污染血脉 / 混血血脉 / 奇异血脉 / 异色 各类型均可正常选择
- [ ] 出货后跳转到结算页，数据正确写入

**S2 赛季专项**
- [ ] 赛季切换器（S1 / S2）可正常切换
- [ ] S2 方案库展示正确（世界池混刷 / 属性混抓 / 单刷奇遇 / 单刷异色）
- [ ] S2 精灵图正常显示
- [ ] 三池进度（家族池 / 系别池 / 世界池）S1/S2 分开统计

**数据与同步**
- [ ] 本地数据读写正常（刷取记录、异色图鉴）
- [ ] 邮箱绑定 / 云端同步无报错
- [ ] 刷取记录赛季筛选正常

**其他**
- [ ] 果实攻略页面正常（S1 / S2 分组）
- [ ] 自定义方案：创建、编辑、删除均正常
- [ ] 更新公告可正常打开

---

## 五、本地 Git Remote 配置

```bash
# 查看当前 remote
git remote -v

# 当前配置：
# origin    https://github.com/05RubyTang/Luoke-yise-test.git     ← 测试站源码
# s2-test   https://github.com/05RubyTang/Yise--assistant-s2.git  ← S2 早期测试（废弃）
```

源码（main 分支）只推送到 `origin`（Luoke-yise-test）：

```bash
git push origin main
```

---

## 六、常见问题

### 构建成功但页面资源 404
原因：`VITE_BASE_PATH` 与仓库名不匹配。  
确认 `deploy:test` 用的是 `/Luoke-yise-test/`，`deploy:prod` 用的是 `/yise-Luoke-v2.0-ruby-s/`。

### 推送失败（权限问题）
```bash
# 检查 GitHub 登录态
git config --global credential.helper osxkeychain
```

### GitHub Pages 未生效
首次推送 gh-pages 分支后，需到仓库 Settings → Pages 确认 Source 设为 `gh-pages` 分支。之后更新会自动生效（约 1 分钟）。
